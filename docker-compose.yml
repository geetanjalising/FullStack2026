version: "3.9"

services:
  backend:
    build: ./backened
    ports:
      - "8007:8007"
    env_file:
      - ./backened/.env

  frontend:
    build: ./frontened
    ports:
      - "5173:5173"
    depends_on:
      - backend